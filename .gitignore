*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode/*
!/.vscode/settings.json
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# PHPStan tmp dir
/.phpstan-cache/

# Rector cache dir
/.rector-cache/

# Infection log
/storage/infection.log

# Pest HTML coverage report (regenerated on every coverage run)
/storage/coverage-html

# Vitest coverage reports (regenerated on every test run)
/coverage

# Octane downloads a local FrankenPHP binary for dev runs — large + platform-
# specific, stays out of git. The worker stub at public/frankenphp-worker.php
# IS committed (the prod Docker build relies on it).
**/caddy
frankenphp

# Track the shareable Claude config (project skills, slash commands, settings)
# but keep machine-local overrides out of git.
.claude/*
!.claude/settings.json
!.claude/skills/
!.claude/commands/
.claude/settings.local.json
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/cost.json
