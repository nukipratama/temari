<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_KEY" value="base64:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="mysql"/>
        <!-- DB_HOST/DB_DATABASE/DB_ANALYTICS_DATABASE/REDIS_HOST come from
             .env.testing (see .env.testing.example), not hardcoded here:
             Laravel swaps in .env.testing whenever APP_ENV=testing (set
             above), the same way .env is per-worktree for dev. Lets each
             worktree point at its own schema on the shared test services
             without editing this tracked file. -->
        <env name="DB_PORT" value="3306"/>
        <env name="DB_USERNAME" value="sail"/>
        <env name="DB_PASSWORD" value="password"/>
        <env name="DB_URL" value=""/>
        <env name="REDIS_PORT" value="6379"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="INERTIA_SSR_ENABLED" value="false"/>
        <!-- Fake Azure config so AnalysisService::autoDispatchEnabled() lets
             dispatch tests fake the bus instead of silently no-op'ing. -->
        <env name="AZURE_OPENAI_URI" value="https://x.openai.azure.com/openai/deployments/x/chat/completions?api-version=2024-10-21"/>
        <env name="AZURE_OPENAI_API_KEY" value="fake-test-key"/>
    </php>
</phpunit>
