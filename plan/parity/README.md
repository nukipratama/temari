# Prototype parity — program orchestrator

A re-scope of the remaining mobile-UX port work, grilled with the user on 2026-08-31.

**This directory is additive.** The original program's tracker at [../README.md](../README.md),
its ledger at [../ledger.md](../ledger.md), and its slice docs under [../slices/](../slices/) are
left exactly as they were. Waves 0-2b happened as recorded there; `V0` closed as recorded there.
What changes is everything *after* that point, and it changes enough to need its own tree rather
than a footnote in the old one.

---

## 1. The constraint

> The frozen prototype at [resources/brand/prototype/](../../resources/brand/prototype/) is the
> **source of truth**. If the prototype doesn't have it, the shipped app doesn't either.

This reverses **decision 5** of the original program ("visual parity is explicitly *not* promised")
and overrides several `ledger.md` verdicts that were final as of 2026-08-28. Every reversal is
recorded in [cut-list.md](cut-list.md) with the verdict it replaces.

It does **not** mean the mockup is copied literally in every respect. Decision P1 below draws that
line precisely.

---

## 2. Decisions

Thirty, settled across eight grilling rounds on 2026-08-31. Same rule as the original program: a
later slice must not silently re-open one of these. To change one, add a row to the amendments log
(§6) and edit the entry here in the same commit.

### Scope and interpretation

| # | Decision |
|---|---|
| P1 | **"Identical" means visual/structural parity, not literal one-for-one.** Every screen renders the prototype's exact layout, sections, order and styling, populated with real data. The plumbing a real app needs but a static mockup cannot draw **survives**: loading states, error states, empty states, AI-pending states, retry affordances, pagination, auth. Any *feature area* the prototype does not draw is still cut. |
| P2 | **Pixel fidelity is token-nearest.** Reproduce the prototype's layout, sizing, spacing and radii as closely as the existing token scale allows, snapping each value to its nearest step. Do not add one-off literal values or weaken `check:palette` / the radius guard to accommodate a hardcoded `rounded-[14px]`. A handful of values landing 1-2px off is accepted; the design-system guards are not. |
| P3 | **Where the prototype draws a control but wires it to nothing, implement the real behaviour.** Its Trends range tabs set state nothing reads; its "load older" reveals swap in a second hardcoded array. Those are mockup limitations, not design decisions: the range tabs genuinely refetch/filter, load-older genuinely pages. |
| P4 | **Cut depth: UI and routes now, backend later.** A cut removes the frontend and makes the surface unreachable. Orphaned models, tables, jobs and services are swept by the original program's `W2` (dead-code sweep), which exists for exactly this. Keeps each PR reviewable and each cut reversible. |
| P5 | **Responsive: adopt the prototype's own model.** ~~The prototype has zero responsive classes.~~ **Corrected 2026-08-31, see §6** — it has a real responsive layer built on **container queries** (`@min-[900px]:`, 93 occurrences across all 11 screens), which a `sm:`/`md:`/`lg:` grep does not find. `PhoneFrame.tsx:47` sets `[container-type:inline-size]`, so it fires at the desktop and wide viewports but not tablet. **One breakpoint, at 900px.** Below it: the single mobile column as drawn. Above it: a centred **760px** column (**520px** Onboarding, **440px** Login's auth card), reduced top padding (`pt-16` → `pt-6`, except Onboarding which does not shrink), and **eleven** wide-only reflows — including Login's feature list stacked → `grid-cols-3` and its CTA row → `inline-grid grid-cols-2`, Settings' HR-zone bounds grid inside `ZonesDisclosure` `grid-cols-2` → `grid-cols-4` (**not** the appearance toggle group, which takes no container query), and Profile's right-aligned block revealed by `hidden @min-[900px]:block`, plus gap, type-scale, border, shadow and background changes. [reference.md](reference.md) holds the full per-screen list; implement from it, not from this summary. Collapse only desktop layout the prototype does *not* specify. |

### Navigation and shell

| # | Decision |
|---|---|
| P6 | **Push-screen nav model, adopted exactly.** Only 5 screens carry the bottom nav: Today, Plan, Race, Trends, History — and **Race lights the `plan` tab**, being a sub-page of Plan. Activity, Inbox, Profile and Settings are *pushed* screens: a dedicated back-chevron topbar, no bottom nav. Login and Onboarding have no chrome at all. |
| P7 | **Nav entry points follow the prototype's topbars.** The brand topbar (on the 5 nav screens) carries a bell → Inbox and an avatar circle → Profile. Profile's own topbar carries a gear → Settings. Every pushed screen gets a back chevron. |
| P8 | **`MeTabs` is cut.** The Profile/Settings tab bar has no prototype equivalent; P7's topbar affordances replace it. |
| P9 | **Desktop `TopNav` is deleted entirely.** The floating bottom pill is the only nav at every width. This **reverts fork 5** of `V0` (merged in [#678](https://github.com/nukipratama/temari/pull/678)), which built a desktop nav bar — a decision made before P5 and P6 existed. |
| P31 | ~~**The content column caps at ~512px.**~~ **Superseded by the corrected P5, see §6.** The width is the prototype's own: **760px** above the 900px breakpoint, **520px** for Onboarding, **440px** for Login's auth card. `PageContainer`'s `max-w-page` / `max-w-page-2xl` and its `sm:`/`lg:`/`2xl:` padding steps are replaced by the prototype's single container-query step. |
| P32 | **The bottom-nav pill is constrained to the content column**, centred, rather than spanning the frame as the prototype's `inset-x-3.5` does. **This is a real divergence, not a gap-filling choice**: the prototype gives `AppTopbar` and `AppBottomNav` zero container queries, deliberately keeping the chrome full-bleed while narrowing content to 760px. We diverge anyway, because at 1536px a full-bleed pill puts its four items uncomfortably far apart. Recorded in [cut-list.md](cut-list.md) §4. |
| P33 | **Back goes to a fixed parent per pushed screen**: Activity → History, Inbox → Today, Profile → Today, Settings → Profile. Not browser history, which does nothing useful when a user deep-links in from a notification or a shared URL. `F4`'s existing `BACK_TARGETS` map is the mechanism. |
| P35 | **Any routed screen named by neither P6 list defaults to pushed chrome** — back chevron, no bottom nav. Currently that means `Collection/Accessories` (already ledger-cut; `W1` deletes it outright). `Activities/Feed` and `Activities/Calendar` are *not* in this category: `History.tsx` renders them behind a `?view=` param, so they inherit History's nav chrome. Operator pages set no layout at all, so P20 holds without action. |
| P34 | **Onboarding moves onto `BareShell`**, the existing chrome-free layout Login already uses. **Entry-chunk budgets in `scripts/check-entry-chunks.mjs` may be re-baselined** as part of this program — the rebrand legitimately changes what each route loads. Re-measure and set honest new numbers; do not delete or weaken the guard itself. |

### Identity and art

| # | Decision |
|---|---|
| P10 | **`TemariProto` is replaced by the prototype's `FaceIcon`.** The prototype's simple line-art ring+face SVG becomes the app's only mascot, used exactly where and how the prototype uses it — **8 of 11 screens** (Today, Plan, Race, History, Activity, Inbox, Profile, Onboarding). It is **absent from Login, Trends and Settings**, and those screens must not gain one. The elaborate mascot asset, its pose system, and the accessory-driven variants are cut. |
| P11 | **The share card carries the Temari logo, not a mascot.** ~~`shareCard.ts` (client canvas) and `RunCardImageRenderer.php` (server) both currently draw the mascot; both switch to the brand mark. They must stay pixel-identical to each other, as they already must today.~~ **Corrected 2026-08-31, see §6** — the server renderer draws no mascot and no glyph (only a `temari.app` text wordmark), and the client was already drawing a *brand mark* (`BrandMark`'s face glyph), not the mascot. The two already diverge on their brand signature. P11 lands as a client-only swap onto the prototype's ring mark; the server is untouched, and closing that divergence is its own decision. |

### Feature disposition

Full per-feature detail, including which `ledger.md` verdict each one reverses, is in
[cut-list.md](cut-list.md). Summarised:

| # | Decision |
|---|---|
| P12 | **Kartu survives as generation + a share button.** Cards are still earned per run. The only way to see one is a share button on its run's detail page, opening the share-card popup. **Cut**: the card-reveal modal, the pending-reveal flow, and the `/api/cards/{card}/seen` + `/replay` routes that serve it. **Kept** (the prototype draws all three): Login's "a card for every run" teaser, History's calendar kartu badge, Inbox unlock rows with rarity badges. There is no collection page to cut — none exists. |
| P13 | **Persona mix is cut, replaced by time-in-zone.** The prototype's Profile hero draws a Z1-Z5 heart-rate-zone bar with a legend in the exact slot the shipped app draws its behavioural persona mix. `PersonaMixTool` goes; the zone data already exists (Settings has a zones editor). Reverses ledger "restyle". |
| P14 | **Badges keep being earned, and surface in exactly two places.** Trends' fitness-panel chips and Inbox unlock rows. **Cut**: the unlock toast, the accessory-unlock modal, any badge gallery, and activity detail's "why this earned Common" explainer. Reverses part of the ledger's "restyle". |
| P15 | **Badge chips show every earned badge, wrapping.** The prototype hardcodes three; a real account earns a variable number, so the row wraps rather than truncating to three. A deliberate, recorded divergence from the mockup's fixed count. **Applies to `PS6` (Trends' fitness panel) and `PS9` (Inbox's unlock rows) only** — the two badge surfaces P14 keeps. **Corrected 2026-09-01, see §6**: this was previously read as covering Profile, which draws no badge chips at all. |
| P16 | **The real Leaflet route map is kept**, styled into the slot the prototype fills with a decorative "activate map" placeholder. That placeholder is a mockup stand-in for a real map, not a decision to have none. Confirms the ledger's "keep, mechanical". |
| P17 | **Dawn-shift is cut.** The light ground becomes a single static palette. Reverses ledger "keep, light-ground only". |
| P18 | **Relative effort is cut.** The prototype's vitals card draws HR avg/max, cadence, steepest grade, flat pace and decoupling — no relative effort. Reverses ledger "keep, mechanical". |
| P19 | **Run lenses are kept.** The prototype's `RunLenses` ("what temari says" narration card with a claims list) is drawn on its activity-detail screen. Confirms the ledger's "keep". |
| P20 | **Operator pages are untouched.** Legal pages stay (the prototype links to them from Settings and the Login footer). Devtools, Devtools/Design and AI-usage stay behind their auth gate — operator tooling sits outside the product surface the prototype specifies, and the AI dead-letter re-arm UI is load-bearing. |
| P21 | **Accessories stays cut**, as already ruled. No change. |

### Screen-level structure

| # | Decision |
|---|---|
| P22 | **Plan gets the prototype's nested timeline.** Week-cluster ("N weeks behind/ahead") → week → planned-vs-actual daily bar chart + day rows → per-day expansion showing a zone-coloured session-segment bar, "temari's take", and a "view activity" link. This replaces the shipped flat day-card list. `S4` skipped this nesting to avoid hiding day-action buttons; P23 removes that objection. |
| P23 | **Plan day-actions: keep Skip and Move, cut Pin, Block and Delete.** Skip and Move **are** in the prototype (`onSkipSession`, `onMoveSession`, a `canSkip`/`canMove` guard, and a weekday-picker for move targets). Pin, Block and Delete appear nowhere in it. Skip and Move live inside the expanded day row, as the prototype places them. |
| P24 | **Season surfaces adopt the prototype's shapes.** Plan gets its `SeasonHeaderCard` (week X of N, adherence %, phase bar, "temari's take") — largely built already by `V0` fork 3. Profile gets its small `SeasonCard` (phase bar + a single progress line). **Cut**: Plan's "Season Track" 5-goal-tier module and `SeasonStreakPanel`'s five-row layout. Season goals still exist in the backend, surfaced as the prototype's single headline line. |
| P25 | **Trends is cut to exactly four blocks**: eyebrow + headline, the range tab bar, the "temari's read" narration card, and one fitness panel (CTL solid + ATL dashed chart, stat tiles, badge chips). **Cut**: milestones, the badge board, strain & monotony, VDOT/pace history, and the personal-bests table. |
| P26 | **Race is cut to three blocks**: race card, projection gauge, goal form. Its CTL/ATL fitness chart is cut — that chart exists once, on Trends, as the prototype arranges it. |
| P27 | **Today's day-grained streak readout is cut.** The prototype's plan card draws a credited/total progress ring and a phase badge with a sparkline, and no "in a row" line. This supersedes part of the 2026-08-30 streak-redesign amendment: "streak" now survives only as the week-streak badge chip on Trends. |
| P28 | **Activity detail is rebuilt to the prototype's section list and order.** Note the earlier `V0` audit was wrong about this screen — the prototype **does** draw the Ask-about-this-run Q&A panel, the narration card, the vitals card, the per-km splits chart and the laps carousel. Genuinely cut: the achievement/collectible block, the "why this earned Common" explainer, and the "Resync from Strava / Send notification" utility row. Also reworked: hero stat hierarchy, and the title / tag-pill / mood-icon treatment. |
| P29 | **Today's "Temari's top pick" featured-card panel is cut** (a Kartu surface the prototype does not draw). |
| P30 | **Demo seed must populate every surviving surface, happy path.** Every screen that survives the cut renders real, representative content for the demo account: a full season with phase variety and a mix of compliance statuses, an active race with a projection, earned badges, inbox variety across kinds, HR zones set, a run with GPS/laps/splits/weather, PR history for the journey chart, and narration filled everywhere. Deliberately-reachable empty states are **not** required. |
| P36 | **Card radius is `rounded-md` (14px), the token step the prototype's `rounded-[14px]` snaps to under P2** — not shadcn `Card`'s `rounded-4xl` (26px). Set by `PS4` as the first screen port and **ratified by merging [#688](https://github.com/nukipratama/temari/pull/688)**; the remaining screen slices follow it rather than each re-deciding. Where a surface is deliberately not a card (hero panels, chips, the nav pill), its own token stands. |
| P37 | **The prototype's lowercase is ported literally, app-wide.** Every string the prototype authors lowercase becomes lowercase: tab labels, button labels, body copy, static captions, the Login hero wordmark, and **data-derived strings too** — dates render `base · 12 jun – 4 sep`, not `Base · Sep 1 – Nov 30`, so backend/format helpers are in scope. **Small mono uppercase labels keep their caps** (`text-label-micro` / `text-label-small`, section eyebrows, stat captions) — the prototype uppercases those itself. `docs/voice-and-tone.md`'s "UI chrome stays Title Case" is **superseded** and must be amended in the same PR, along with the matching line in `.claude/skills/temari/SKILL.md`. Ruled by the user 2026-09-01 after the post-port browser sweep found the divergence on five screens independently. Owned by `PS13`. |
| P38 | **History's `RecapCard` keeps all six metrics, restyled onto the prototype's chip shape.** The prototype draws two compact narrative chips (`HistoryScreen.tsx:416-424`); `PS7` reused the shipped six-badge `WeeklyStatusChips` strip, which carries monotony, drift and readiness the prototype has no slot for. Neither the strip's density nor dropping three real metrics is acceptable: the six survive, redrawn in the prototype's compact chip treatment. Ruled by the user 2026-09-01. Owned by `PS14`. **Premise corrected 2026-09-02, see §6**: `WeeklyStatusChips`' `CHIP_BASE` is already byte-identical to the prototype's chip class, so the redraw is a no-op. The real divergences are colour and row layout, and the user's ruling on them is in [PS14-screen-local-fixes](slices/PS14-screen-local-fixes.md) — the always-on Fitness / Readiness / form-status tints drop to uniform `bg-muted`, the threshold-triggered Monotony and Drift alert tints stay, and the chips row becomes `justify-between` with the send button the prototype draws as a circular bell. |

---

## 3. Where the earlier audit was wrong

Recorded because two of these directly shaped decisions the user made, and because the same mistake
is easy to repeat.

`V0`'s comparison sweep screenshotted the prototype at its **fixed 844px frame height**, capturing
only what was visible without scrolling, and never opened its collapsed disclosures. Three things
were reported as "absent from the prototype" that are in fact present:

- **Activity detail's Q&A panel, narration card, vitals card, splits chart and laps carousel** are
  all drawn by `ActivityDetailScreen.tsx`. The page being long is not itself a divergence.
- **Today's weekly stats, the three vital bars, the last-run card and the condition card** are all
  drawn — inside the prototype's own collapsed "this week's stats" disclosure.
- **Kartu** is referenced in three places (Login teaser, History calendar badge, Inbox unlock
  rarity), so it was never a clean cut.

A full-scroll capture across all five viewports is the first thing `PP0` does, so the parity work
starts from a correct picture rather than this one.

---

## 4. Slice map

Foundation first (these are cross-cutting and would collide across parallel worktrees), then one
slice per screen, run 3-at-a-time in worktrees as every prior wave did.

```
foundation ──  PP0 → PP1 → PP2 → PP3      (serialized; main checkout)

screens    ──  PS1 … PS11                  (3 parallel worktree slots)

close      ──  PP4                         (demo seed, after every screen slice)

then       ──  the original program's W1 → W2 → W5   (wave 3 cleanup, unchanged)
```

| id | name | what it does |
|---|---|---|
| `PP0` | Full-scroll reference capture | Capture every prototype screen at full scroll height across all 5 viewports, with disclosures opened. The reference every screen slice measures against. No app code. |
| `PP1` | Shell, nav and responsive model | P5-P9: single fluid column to a max-width, push-screen nav model, topbar entry points, delete `TopNav` and `MeTabs`, revert fork 5. Touches every screen's chrome, so nothing else runs concurrently. |
| `PP2` | Identity: FaceIcon and share card | P10-P11: replace `TemariProto` with `FaceIcon` everywhere; re-cut the share card onto the brand mark, client and server in lockstep. |
| `PP3` | The cut | P4-depth deletion of every cut surface in [cut-list.md](cut-list.md): reveal modal + its API routes, persona mix, dawn-shift, relative effort, unlock toast, badge gallery, Today's top-pick panel, the resync/notify row, Trends' and Race's cut sections, Season Track, `SeasonStreakPanel`'s tier rows. UI and routes only. |
| `PS1`-`PS11` | Per-screen exact match | One per prototype screen (Login, Onboarding, Today, Plan, Race, Trends, History, Activity, Inbox, Profile, Settings). Each reworks its screen to the prototype's section list, order and treatment at P2 fidelity, against `PP0`'s reference captures. `PS4` (Plan) is the largest — it builds P22's nested timeline. |
| `PP4` | Demo seed completeness | P30: every surviving surface populated for the demo account. Runs last, once the cut list and every screen are settled, so it seeds the real final shape. |

`W3` (coverage reconciliation) from the original program is **dropped** — it existed to pay down
coverage debt across screens this program now rewrites; each slice here carries its own coverage.
`W1`, `W2` and `W5` still apply and run after `PP4`.

---

## 5. Progress

Status vocabulary: `todo` · `in-progress` · `in-review` · `merged` · `blocked` · `cut`.

| id | name | status | PR | slot | notes |
|---|---|---|---|---|---|
| PP0 | Full-scroll reference capture | merged | [#681](https://github.com/nukipratama/temari/pull/681) | main | 90 captures + [reference.md](reference.md); found 5 factual errors in this plan; squashed as 1e45a122 |
| PP1 | Shell, nav, responsive | merged | [#682](https://github.com/nukipratama/temari/pull/682) | main | [PP1-shell-nav](slices/PP1-shell-nav.md); `TopNav`+`MeTabs` deleted, push-screen nav, 900px/760px layer across 24 files; 8 of 11 wide-only reflows carried, 3 deferred to `PS1`/`PS11`; no budget re-baselined; squashed as ed227cc1 |
| PP2 | FaceIcon + share card | merged | [#684](https://github.com/nukipratama/temari/pull/684) | main | [PP2-faceicon-sharecard](slices/PP2-faceicon-sharecard.md); 14 commits, 87 files, −3875 lines; squashed as 1da1aebb; `FaceIcon` on all 8 prototype screens, removed from Login and from the three cards the prototype draws faceless; one brand mark (`TemariMark`) now, on the share card too; four plan errors found — **P11's server half does not exist** (`RunCardImageRenderer` draws no mascot, only a `temari.app` wordmark) and `shareCard.ts` already drew the brand mark, so the change is client-only and the PHP renderer is byte-unchanged; both forks resolved (Accessories page deleted, `Devtools/Design` respecimened); coverage 97.25% → 97.15%; two CI-only guard failures fixed on the branch (orphaned `aksesori` `ALLOWED` entry, a `{@see}` at the deleted `AccessoryController`) — the trigger for slice `C1` |
| PP3 | The cut | merged | [#683](https://github.com/nukipratama/temari/pull/683) | main | [PP3-the-cut](slices/PP3-the-cut.md); 12 commits, 124 files, −7057 lines; every §1 row cut except `TemariProto` (`PP2`), Onboarding chrome (`PP1` already did it) and Accessories (`W1`); one cut-list error found (dawn-shift's `shareCard.ts` consumer is a colorway id, not a consumer); coverage 95.51% → 97.25%; squashed as e9309b6b |
| PS1 | Login | merged | [#686](https://github.com/nukipratama/temari/pull/686) | 3 | [PS1-login](slices/PS1-login.md); prototype section list + both why-list states, no `FaceIcon`; `PP1`'s deferred reflow #2 resolved by measurement (flat `text-display-sm`, +5.2/+6px at 1280/1536 recorded); Login chunk 146.7 → **140.3 kB gz** of 160; coverage flat 97.15%; four plan/prototype discrepancies found — `PP1` read reflow #5 only at its wide half and left the hairline state unbuilt, and its `max-w-[760px]` hero cap is not the prototype's; squashed as 963ff5fe |
| PS2 | Onboarding | merged | [#696](https://github.com/nukipratama/temari/pull/696) | 1 | [PS2-onboarding](slices/PS2-onboarding.md); the last screen slice. P34/P5/P10's two `FaceIcon` placements and **P3** all verified already-satisfied rather than redone (P3 pre-paid, as on `PS5`); **P36 had never reached this page** — it was the last screen still mounting shadcn `Card` at `rounded-4xl`, twice, plus three `rounded-xl` surfaces, all now `rounded-md`. One half of `reference.md` §1.1's Onboarding row **was unimplemented**: the page carried `PageContainer`'s `py-6`, so `pb-10` → `pb-16` never shipped. Added: both radial glows, the two-line accent headlines, the missing goal-step recap line, the inline "optional" chip, and the prototype's staggered option reveal; the goal form adopts `PS5`'s `inputVariants`/`outlineChipVariants` for the same prototype form. **`IconChoiceCard` is shared with Settings and its radius moved there too** — a deliberate P36 consequence, not a side effect. Two deliberate non-copies: the app's skip affordances stay (the prototype's preferences step cannot be declined, and here every answer drives plan generation), and the pace readout is mono not serif. `reference.md` §4 verified accurate throughout. Coverage 97.63% → **97.64%**, up on all four axes.; squashed as 19e19a34 |
| PS3 | Today | merged | [#691](https://github.com/nukipratama/temari/pull/691) | 1 | [PS3-today](slices/PS3-today.md); 35 files, prototype's four sections, stats disclosure **closed** per §6's amendment (d); `VitalChips`→`VitalBars`, both mini cards reworked, `NoPlanCard` added; P27/P29 verified absent rather than redone; two findings — `PP1` put reflow #8's `gap-6` on a `flex-col` where the prototype has a row, and `reference.md` §5's disclosure row still read as if open-by-default shipped; coverage 97.30% → 97.39%; `Home.tsx` 200.8 kB gz of 240; squashed as 4f64cb04 |
| PS4 | Plan | merged | [#688](https://github.com/nukipratama/temari/pull/688) | wt | [PS4-plan](slices/PS4-plan.md); largest slice — 44 files, 11 commits; P22's nested timeline, P23's Skip/Move-only day actions, P24's `SeasonHeaderCard`; **Move is now a swap** (the old re-date hit `planned_sessions`' `(user_id, date)` unique constraint on every in-horizon target); `WeekPlanDay` gained `actual_km`+`activity`, so Today's fixtures moved (`WeekPlanWidget` itself unchanged — `PS3` should know); card radius `rounded-md` vs the shadcn `Card`'s `rounded-4xl` is a **cross-screen call** left open; coverage 97.15% → 97.30%; caught and fixed a `C1` regression — `composer check` could not complete, composer's 300s process-timeout killed phpstan, and its first end-to-end run found 7 real failures; squashed as c0672c02 |
| PS5 | Race | merged | [#694](https://github.com/nukipratama/temari/pull/694) | 1 | [PS5-race](slices/PS5-race.md); the prototype's section list, with the schedule/race tabs moved **below** the intro where `RaceGoalScreen` puts them; `PlanRaceTabs` onto the prototype's segmented track (`HistoryNav`'s shape, from the same prototype element), which orphaned **`SectionTabs`** — deleted, as `PP1` predicted; three prototype-named components out of the page body (`RaceCard`, `ProjectionBlock`, `RaceGoalForm`); P26 verified rather than redone, and P3 was already satisfied — the mockup's three dead controls were live before this slice; the `AiReplanPill` cooldown deliberately not ported (no rate limit exists on `POST /race`; replan lives on Plan behind its own); one plan error found — **`cut-list.md` §1's Race row is wrong about where the cut chart went**, `PS6` built its own so `race/CtlTrendChart.tsx` is now orphaned, not "surviving on Trends"; `reference.md` §7 verified accurate throughout; coverage 97.55% → **97.63%**, up on three of four axes; squashed as 509c7c10 |
| PS6 | Trends | merged | [#692](https://github.com/nukipratama/temari/pull/692) | 1 | [PS6-trends](slices/PS6-trends.md); the prototype's four blocks, P3's tabs now really re-window narration + chart + tiles + chips, P14/P15's wrapping chip row; **`PP3` left a fifth block** — `StreakBadge` — which P25 does not allow and P27 keeps only as a chip, so it is deleted and folded into `FitnessPanel`; `FitnessTrend`→`FitnessPanel` and `NarrationHeadline`→`NarrationCard` on the prototype's vocabulary; one backend change, `firstEarnedDatesForUser`→`firstEarnedBadgesForUser` carrying rarity so the chip medals tint from real data; one plan finding — `reference.md` §8's absent-list misses `StreakBadge` entirely; coverage 97.41% → **97.55%**; squashed as 2710e115 |
| PS7 | History | merged | [#693](https://github.com/nukipratama/temari/pull/693) | 2 | [PS7-history](slices/PS7-history.md); P3's load-older is a real `?weeks=` page (`FeedQuery::weekWindow`, week-grained), replacing the client reveal and the `MAX_RUNS=365` cap; the calendar is rebuilt onto the prototype's per-week box grid with a tap-to-expand recap disclosure, and **P12's kartu badge lands inside that disclosure** (week's rarest card), never on a day cell; one shared `HistoryHeader` for both `?view=` halves, which had drifted apart; week rows total the whole Mon-Sun week while the month meta stays month-scoped (the prototype's own meta sums its rows and so counts July days in August) — recorded, not followed; one plan error found — **`EmptyRunsState` is not on History** (`Home.tsx` is its only consumer), so the brief's second `FaceIcon` placement is `EmptyPanel` at 48; `reference.md` §9 verified accurate throughout; coverage flat 97.41% with branches 91.21 → 91.31; squashed as 6d8503f4 |
| PS8 | Activity detail | merged | [#687](https://github.com/nukipratama/temari/pull/687) | 2 | [PS8-activity-detail](slices/PS8-activity-detail.md); 15 commits, 44 files; rebuilt to the prototype's section list — all five sections `V0` wrongly called absent kept; four renames to the prototype's vocabulary (`PastYouHero`→`PastYouCard`, `DetailTiles`→`VitalsCard`, `SplitsTable`→`SplitsChart`, `LapsGraph`→`LapsCarousel`) plus a new `RunHero`; the on-page kartu block cut, orphaning `Kartu`/`KartuMount`/`ZoneBar` (share path via `ShareCardModal` verified untouched); no plan/prototype discrepancy found; coverage 97.15% → 97.21%; `Runs/Show` 206.3 kB gz against a 245 kB budget, no re-baseline; squashed as a045e936 |
| PS9 | Inbox | merged | [#695](https://github.com/nukipratama/temari/pull/695) | 2 | [PS9-inbox](slices/PS9-inbox.md); the prototype's section list and row anatomy, incl. its post-run stat chips; P3's load-older is a real `?shown=` growing window (`hasOlder` flag, deep link widens the window instead of resolving a page), replacing the two-way Newer/Older pager; **P12's rarity badge had never rendered** — the branch existed but `UnlockGrantedNotification`'s payload carries no rarity and the only payload `rarity` is the *card* rarity on post-run rows, which the `kind === 'unlock'` guard excludes, so it is now resolved read-side from `config/temari_unlocks.php` by `unlock_key` (rates every already-recorded row); chips adopt `PS6`'s treatment verbatim (muted ground, rarity in the medal ink); P14's cuts verified clean, unlike Trends; two plan findings — **P15 has no chip surface on Inbox at all** (its badge surface is the unlock *row*, so P15 reduces to "nothing truncates") and `PP3`'s demo seed leaves unlock rows unqueued, flagged for `PP4`; `reference.md` §11 verified accurate throughout, and `PP1` correctly lists no Inbox reflow; coverage flat 97.55% with branches 91.39 → 91.43; squashed as f67fc94c |
| PS10 | Profile | merged | [#689](https://github.com/nukipratama/temari/pull/689) | 2 | [PS10-profile](slices/PS10-profile.md); prototype section list, P13's Z1-Z5 time-in-zone bar (new `TimeInZoneSummary` over `stream_summary`'s per-run zone minutes) and P24's small `SeasonCard` (`peekCurrent`, never `ensureCurrent`); hero moved from the sky-gradient panel to the prototype's own card tone, as `PS8` did; `ProgressionChart` replaced in place by an inline-SVG `JourneyChart`, orphaning it (`LineChart` stays — Trends and Race still lazy-load it); one plan error found — **P15 has no surface on Profile**, the prototype draws no badge chips there and `cut-list.md` §2 puts them on Trends (`PS6`) and Inbox (`PS9`); squashed as 6fe4b763 |
| PS11 | Settings | merged | [#690](https://github.com/nukipratama/temari/pull/690) | 3 | [PS11-settings](slices/PS11-settings.md); both `PP1`-deferred reflows resolved — **#10's premise was false** (`SettingsScreen.tsx:473-490` draws two inputs in that grid, not the three `PP1` recorded, so the wide step was portable verbatim with no data change), #11 landed with the `AccountActions` restructure into the prototype's button pair; training preferences becomes the prototype's always-open card on the three controls Onboarding already shares with it (`TrainingPreferencesDisclosure` → `TrainingPreferencesCard`) and moves above the zones disclosure; the zones editor drops ten bound inputs to five, since `UpdateHrZonesRequest` already forces `hi[i] == lo[i+1]` (client-only, request class byte-unchanged); found `epic/mobile-ux-port` **already red at 7a8d54c9** on an orphaned `foreground/0.06` grounds registration, fixed in its own commit; coverage 97.37% → **97.52%**, up on all four axes; no browser pass run; squashed as 85ab8b9b |
| PP4 | Demo seed completeness | merged | [#697](https://github.com/nukipratama/temari/pull/697) | main | [PP4-demo-seed](slices/PP4-demo-seed.md); squashed as 4f378a62. Ran **before** `PS12`/`PS13`, reversing the slice map: five of its gaps were surfaces no screenshot could reach, so settling treatment first would have meant re-reviewing every screen after. **Two findings beyond the seed data.** (1) `demo:seed` did **not** converge, and its own log line said `0 accessory unlocks granted (all already unlocked)` on a database with zero unlock inbox rows — honest about the wrong thing. `GrantEligibleUnlocksAction.php:51` short-circuits once every catalog key is granted, so a database seeded before unlock notifications existed could never gain its rows. Both `PS9`'s recorded cause and this slice's first hypothesis were wrong; an empirical test settled it, and the stale comment at `DemoRunSeeder.php:154` that misled both is corrected. (2) **Ordering was a second, hidden bug**: `InboxController.php:45` paginates on `orderByDesc('id')` while the page buckets on `created_at`, which only agree while rows are inserted chronologically — automatic in production, false once rows are backdated. The first fix wrote them out of order and **an entire bucket stopped rendering**; entries are now collected, sorted by date, then written, with the invariant asserted rather than assumed. `grade_smooth` was missing from the synthesizer entirely, so `StreamAnalysis` computed no hill metrics for any run; the daily keep-alive's elevation floor was raised (measured across 14 days, 3.3-7.6%, kept near the season's own 4.3% max) so the newest run — the one a reviewer lands on — clears the 3% gate every day rather than intermittently. **`goal_sec` left the slice**: `ProfileController.php:149` hardcodes it to `null`, so the prototype's goal chip can never render for any user on any data, which is a dead branch rather than missing data — reassigned to `PS14`. Onboarding stays unreachable by design, recorded as a structural exception. One app change: `GrantEligibleUnlocksAction::celebration()` private → public, no behaviour change. Full suite green, 3660 tests. Original gap list below. **Seven P30 gaps, all confirmed live by the 2026-09-01 browser sweep** (the row previously recorded only the first): (1) `demo:seed` grants 21 unlocks but `UnlockGrantedNotification` is `ShouldQueue`, so with no worker the demo inbox holds 2 rows and P12's unlock surface is invisible (found by `PS9`); (2) the demo user has **no `training_preferences` row at all**, so Settings' whole preferences card runs on fallbacks, `run_days` is empty and the "which one's the long run?" block (`TrainingPreferencesCard.tsx:271`, gated on selected days) never renders; (3) no `goal_sec` on the journey series, so `ProgressionCard.tsx:95`'s goal chip never renders; (4) the sampled run is flat, so `VitalsCard.tsx:102-120`'s steepest-grade and flat-pace tiles never render (gated on `max_grade_pct >= 3`); (5) no run questions exist, so `AskAboutRun.tsx:109-121`'s prior-questions list never renders; (6) the inbox window has nothing older, so `Inbox.tsx:144-164`'s load-older control never renders; (7) `DemoRunSeeder.php:645-650` heals the demo user to onboarded on every re-seed, so **Onboarding is unreachable for demo** — a structural exception to P30, not a fixable gap, and the reason a browser sweep can never cover that screen without a second fixture user. Of these, (3) was reassigned to `PS14` as a dead branch and (7) stands as the exception; the other five are closed. |
| PS12 | Cross-screen treatment reconciliation | merged | [#700](https://github.com/nukipratama/temari/pull/700) | main | **deferred by the user 2026-09-01**, after all eleven screens land. Holds treatment calls that span already-ported screens and cannot be settled by one slice. Currently one: the **regenerate control** is a bottom-right pill in the prototype, a left-aligned text trigger in `AnalysisStatus`, which six ported screens share (raised by `PS6`). Second: the **empty-state shape** — the prototype draws a compact horizontal card with a 40px `FaceIcon` (`HistoryScreen.tsx:509-529`), the app draws `EmptyPanel`, a centred panel with a 48px face, across eleven call sites (raised by `PS7`). Deferred deliberately so `PS2`/`PS5`/`PS9` can first show whether the prototype is even consistent about them. **`PS5`'s answer: it is not.** Race's `NoRaceState` (`RaceGoalScreen.tsx:226-243`) is a *centred* card — face on top, serif-italic title, a `max-w-[220px]` body — which is `EmptyPanel`'s shape, not History's horizontal one. Only the 40px face size is common to both. So the call is per-screen shape, not one shape the app got wrong. **`PS9`'s answer agrees with History, not Race**: `EmptyInboxCard` (`InboxScreen.tsx:261-281`) is the same compact *horizontal* card + 40px `FaceIcon`. So the split is two-to-one — History and Inbox horizontal, Race centred — which sharpens `PS5`'s reading rather than reversing it: the shape is per-screen, and the 40px face is the only constant. What is still one call is that constant, against `EmptyPanel`'s 48. Third: the **active tint on the three shared preference controls** (`IconChoiceCard`, `SessionsDial`, `DayPicker`), which Settings and Onboarding both render — the app paints `bg-horizon/10`, the prototype `bg-horizon/20` (`PreferenceControls.tsx:67`, `:133`), and the prototype's local `PillOption` agrees with itself at `/20`. Raised by `PS2`, which left `/10` standing: `PS11` shipped it, `outlineChipVariants` uses it, and moving it needs a new `horizon/0.2` registration in `grounds.json` plus a visual change on a merged screen. A one-value call spanning two screens, so it belongs here rather than in either. Add further cross-screen calls here rather than settling them inside a screen slice. | **Verified against prototype source 2026-09-02 before implementing; two of the three premises changed**, and the slice doc [PS12-cross-screen](slices/PS12-cross-screen.md) carries the corrected reading. (1) The regenerate control: the prototype draws **two** different affordances under that name, and only one is `AnalysisStatus` — the narration reread, which `TrendsScreen.tsx:146-181` and `ActivityDetailScreen.tsx:583-666` draw **byte-identically**, so it is consistent after all and ports as written. The other is the whole-object replan (Plan's header, Race, Settings), whose Plan instance `PS14` already shipped. (2) The empty state: the remaining "one call" — 40px against `EmptyPanel`'s 48 — **is not one call**. The prototype draws 40 on Inbox, History and Race and **48 on Plan**, so both the face size and the layout are per-screen, and `EmptyPanel` is accidentally right on exactly one of its four face-bearing call sites. (3) The tint: `SessionsDial` has **no** horizon tint in either the prototype or the app — it fills solid and already matches — and a blanket `/10` → `/20` sweep would have **broken** `LapsCarousel`, which draws `/10` because `ActivityDetailScreen.tsx:985` does. The prototype's `/20` is its selected-*control* tint; its `/10` and `/14` are decorative. Squashed as 322c7c9b. **A fourth finding on the tint, after the first three**: registering `horizon/0.2` failed `DesignTokenContrastTest` on a real accessibility result — `horizon-ink` on it over paper scores **4.48:1** against AA's 4.5, and `icon-accent` fails with it, the prototype being dark-first and its light ground never contrast-audited. Settled by **P2** without a new decision: snap to the nearest step the guards accept, never weaken the guard, so the three controls took `horizon/[0.18]`. Recorded as a divergence from the prototype's literal value rather than as parity. `DesignTokenContrastTest` caught three separate registration gaps across the slice, each fail-closed. **One divergence is left standing deliberately**: `Card`'s `empty` tone is `border-dashed bg-card/40` where every prototype empty state is a solid `border-border-strong bg-card shadow-e1` card — a fourth cross-screen call, spanning all nine `EmptyPanel` sites, that no slice has raised, so it was flagged rather than folded in. The four empty states could not be browser-verified: the demo account has data on every one of them, so they are covered by unit assertion instead.
| PP5 | Dark-ground `horizon-ink` + calendar Today button | merged | [#702](https://github.com/nukipratama/temari/pull/702) | main | **A bug in merged work, not a parity item** — reported by the user against the running app on 2026-09-02. `F2` excluded `horizon` from the dark `-ink` inversion on the stated reasoning that the app reaches for `icon-accent` on dark instead; **33 `text-horizon-ink` call sites disprove it**, and they were rendering `#546d23` on `#0b1017` at **2.9:1**. This is **R1's** predicted failure mode arriving eighteen slices late: `DesignTokenContrastTest`'s dark-ink check iterates only the tokens the dark block *redefines*, so omitting the token removed the only guard that could have caught it. The check now asserts the roster instead. Carries the user's cut of the calendar's Today button, which the prototype does not draw either. Also found, not touched: `resources/brand/tokens.css` and `tokens.html` still hold the **pre-Pewter gold-and-indigo palette** and nothing reads them — flagged for `W2`. [PP5-dark-ink-fix](slices/PP5-dark-ink-fix.md) | Squashed as 6d4fa105.
| PP6 | Favicon and app icons | merged | [#703](https://github.com/nukipratama/temari/pull/703) | main | **Not a parity item** — the user asked for the bunny favicon to go. Two problems, only one of them the drawing: `public/favicon.svg` was a **different mark entirely** (a bunny, on the pre-Pewter indigo and gold), while `resources/brand/logo/` **already held the arc mark** in all six of its SVGs, just still painted in the old palette. So the logo needed recolouring, not redrawing, and `public/` needed to use it. Every raster now derives from those SVGs through a new `build-icons.mjs`. **The differing arc counts are deliberate and were left alone**: three arcs at app-icon size, two at favicon size, which is what `TemariMark.tsx` itself renders in-app — a logo system, not an inconsistency. **Two dead PWA shortcuts fixed on sight**: `History` pointed at `/activities`, which is not a route (the same wrong assumption that had been dropping activity detail from every browser sweep until `PP4`), and `Collection` pointed at `/cards`, which has never existed at all (P12). Both shortcuts were followed and now return 200. Recorded, not taken: whether the PWA should launch on the dark ground, and the `text/plain` MIME the manifest is served with (a Caddyfile matter, not application code). [PP6-brand-icons](slices/PP6-brand-icons.md) | Squashed as 7ce5f47e.
| PS13 | Lowercase copy pass | merged | [#698](https://github.com/nukipratama/temari/pull/698) | main | [PS13-lowercase](slices/PS13-lowercase.md); squashed as 22572f80, 104 files. **The browser pass is what made it correct.** A line-based scan found 292 candidates but is structurally blind to multi-line JSX, so `Save changes` / `Log out` / `Delete account` / `Planned`+`Actual` all survived the first pass; and `pace.ts` was treated as *the* date choke point when `verdict.ts:133` and `plan.ts:161` each format dates themselves — Today read "faster than you were in **April**" directly above "pace vs **apr** 5". **Two mechanical attempts at the 197-test churn were reverted**: a blanket substring replace corrupted identifiers by matching `Sync`, `Card` and `Ask` inside `StravaSyncBadge`, `ShareCardModal` and `AskAboutRun`; expectations were finally driven from the suite's own failure output, round by round. Deliberate non-changes, each recorded: CSS-uppercased mono labels (source case invisible, and the ruling kept their caps), proper nouns and domain acronyms (an early codemod wanted to lowercase *Jack Daniels* and *Edwards*), Strava's brand-prescribed button wording, Strava-authored activity titles, and **inline legal/safety copy** (`DataUseStatement.php`, `TrainingDisclaimer.php`) — the prototype draws no equivalent and lowercasing a medical disclaimer weakens it, reversible in two constants. `docs/voice-and-tone.md` and `.claude/skills/temari/SKILL.md` both amended. Frontend 1834 tests green, PHP 3660 green, chunks unchanged. Open question left open: the prototype **contracts** where the app expands (`can't` vs "cannot"), which is register rather than casing. |
| PS14 | Screen-local parity fixes | merged | [#699](https://github.com/nukipratama/temari/pull/699) | main | The seven single-screen divergences the 2026-09-01 browser sweep confirmed, each settled and none cross-screen (so they do not belong in `PS12`): (1) Login's hero wordmark renders `Temari` where the app's own topbar renders `temari` on every other screen — an internal inconsistency, fixable ahead of and independently of P37; (2) Login's kartu teaser thumbnail measures 130x98 live against the prototype's `h-21 w-19.5` = 78x84 (`LoginScreen.tsx:165-189`); (3) Inbox's "open" link drops the trailing `ArrowRight` the prototype draws (`InboxScreen.tsx:239-245`); (4) Plan's header AI-replan trigger is a ghost `PillButton` (`Plan.tsx:128-149`) where the prototype draws a filled `bg-muted` uppercase-mono pill (`PlanScreen.tsx:1742-1752`) — **distinct from `PS12`'s `AnalysisStatus` control**, which is a different trigger on a different surface; (5) Today's plan-card phase badge lost the `uppercase` the prototype gives it (`TodayScreen.tsx:236` vs `WeekPlanWidget.tsx:219-221`); (6) History's `BellPlus` is a full-width labelled pill on its own row against the prototype's small circular icon button inline with the chip row (`HistoryScreen.tsx:426-431`); (7) Settings' push row uses a message glyph where the prototype uses `Smartphone` (`SettingsScreen.tsx:269-283`). Also carries **P38**: `RecapCard`'s six metrics redrawn onto the prototype's compact chip shape. **Plus one reassigned from `PP4`**: Profile's `goal: sub-50:00` chip (`ProgressionCard.tsx:95`) is gated on `series.goal_sec`, which `ProfileController.php:149` hardcodes to `null` — a dead branch that renders for no user on any data. Wiring it means choosing a goal source (race goal? season goal? a PR target?), so it needs a decision before it is either wired or cut. | **Verified against source 2026-09-02 before implementing, and four of the eight items were wrong or understated**; the corrections, the two user rulings they needed, and an eighth item are in [PS14-screen-local-fixes](slices/PS14-screen-local-fixes.md). (1) Login's wordmark **already landed in `PS13`**. (2) Login's teaser is not a sizing bug: the prototype draws a decorative 78x84 tile, the app draws the real `KartuMini` at a hard-fixed `w-[140px]`, and the recorded `130x98` appears in neither source — **ruled**: a compact `KartuMini` mode in the prototype's slot, on P16's precedent. (5) Today's phase badge is genuinely not uppercase, but the cause is broader than the call site: `chipVariants` names `text-label-micro` in its base and `text-[11px]` in its default `sm` size, and `cn.ts` registers `text-label-micro` in tailwind-merge's `font-size` group, so **no `Chip` in the app has ever rendered mono, uppercase or bold**. Confirmed by `getComputedStyle`, not by reading source, which had concluded the opposite. (6) History has no bell at all: the affordance is `SendNotificationButton` (`mdi:send`, labelled, own row, gated on done + reachable), reshaped here into the prototype's inline circular button with its states kept. (8) P38's premise is a no-op and is amended in §2. (9) `goal_sec` — **ruled**: the active `RaceGoal`, matched within the builder's own +/-5% band; `season_goals` was checked and cannot express a per-distance time target. It also carries a **new eighth item**: `demo:seed` creates no `RaceGoal` at all, so the goal chip and Race's own populated state are both invisible on a fresh database — `PP4`'s exact failure mode, and `PP4`'s own comment asserts a race "seeded alongside" that does not exist. Squashed as abf51bfe. **One further finding, and it is the widest in the slice**: no `Chip` in the app has ever rendered as a label — `chipVariants` names `text-label-micro` in its base and `text-[11px]` in its default `sm` size, and `cn.ts` registers `text-label-micro` in tailwind-merge's `font-size` group, so the size variant strips it at every call site. Deliberately **not** fixed by restoring the base, since the prototype uses both a mono-uppercase and a sans sentence-case chip; the base drops a class that never applied (zero rendered change) and the one call site that wants the label tier passes it itself. P38's uniform chips orphaned four `grounds.json` panel registrations, removed surgically. All eight items verified live at 390px by DOM query rather than screenshot read; zero console errors, zero overflow at 320/390. Frontend lines 97.86%.

---

## 6. Amendments

Every deviation from §2 lands here, dated, with the reason. Empty is the healthy state.

| date | decision | change | why |
|---|---|---|---|
| 2026-08-31 | 5, 10, 12, `V0` fork 4 | **Four further factual errors corrected, all found by `PP0`'s source cross-check.** (a) P5 said "four wide-only layouts"; there are **eleven**, and Settings' `grid-cols-2` → `grid-cols-4` is the HR-zone bounds grid, not the appearance toggle; Onboarding's `pt-16` does not shrink. (b) P10 said `FaceIcon` is on "9 of 11 screens"; it is on **8**, and is absent from Login, Trends and Settings. (c) `cut-list.md` presented Kartu's share button as prototype-backed; the prototype draws **no share button and no dialog anywhere**. The button stays, reclassified as a deliberate divergence (§4) so a generated card is viewable at all. (d) Today's stats disclosure renders **closed** in the prototype (`TodayScreen.tsx:464`, no `defaultOpen`); this **supersedes `V0` fork 4**, which chose open-by-default under the old parity-optional rule and partly on a screenshot that never opened it. | `PP0`'s brief made cross-checking `cut-list.md` against prototype source a primary deliverable, precisely because P5's earlier error had shown the plan's factual claims needed independent verification. It found five discrepancies; four are corrected here and one (VDOT/PR history surviving on Profile) was a scoping ambiguity clarified in place. Decisions (c) and (d) went back to the user rather than being patched silently. |
| 2026-08-31 | 5, 31, 32 | **P5's central factual claim was false and is corrected.** P5 asserted the prototype had "zero responsive classes ... content never reflows". It does reflow: it uses **container queries** (`@min-[900px]:`, 93 occurrences across all 11 screens) rather than breakpoint prefixes. P5 now describes the prototype's real model (900px breakpoint; 760/520/440px columns; four wide-only layouts). P31's invented 512px column is superseded by the prototype's own 760px. P32 survives, but is reclassified from "filling a gap the prototype left" to a **deliberate divergence against a choice the prototype actually made** — it gives its chrome no container queries on purpose. [cut-list.md](cut-list.md) §4's corresponding row is corrected too. | The claim came from a grep run during the grilling session that tested for `sm:`/`md:`/`lg:`/`xl:` prefixes and for `@sm`/`@md`/`@lg`/`@container`, but not for the arbitrary-value form `@min-[900px]:` the prototype actually uses. It returned zero and was reported to the user as fact; three decisions were then made on top of it. Caught by the `PP1` agent, which stopped before writing any code rather than implementing against a premise it had verified as false, and re-verified independently against the `prototype-frozen` tag before the correction landed. |
| 2026-08-31 | 11, cut-list §1 `TemariProto` row | **P11's premise is factually wrong on both halves, corrected here.** P11 says `shareCard.ts` and `RunCardImageRenderer.php` "both currently draw the mascot" and "must stay pixel-identical to each other, as they already must". Neither holds. (a) `RunCardImageRenderer.php` draws **no mascot and no glyph at all** — its only non-text mark is the rarity flag's star `<polygon>` (`:547`), and its brand signature is a `temari.app` text wordmark (`:487`). (b) `shareCard.ts` was already drawing a **brand mark**, not the mascot: `temariSvg()` is a canvas port of `BrandMark.tsx`'s `TemariGlyph`, mislabelled by two stale "mascot watermark" comments. (c) The two therefore **already diverged** on the brand lockup — client: mark + "Temari" top-right plus a mood-tinted mark in the art window; server: a bottom-right `temari.app` stamp. P11 is implemented as a **client-only** change (the face glyph → the prototype's ring mark, geometry pinned via an exported `brandMarkSvg`); the PHP renderer is byte-unchanged. Separately, the cut-list row's `build-mascot.mjs` is **kept**: it is a brand generator rather than UI, and `build-accessories.mjs` — which the Accessories row assigns to `W2` — imports it, along with three preview generators. | Found by `PP2` reading the two renderers before editing either, as the standing rule requires. Acting on the claim would have meant inventing server-side geometry to "match" a client change, which increases divergence and drags a `MIRROR_FILES` backend file into a frontend diff. Bringing the server's signature into line with the client's is a real, separate decision — recorded as [PP2-faceicon-sharecard](slices/PP2-faceicon-sharecard.md) open question 1, not taken here. |
| 2026-08-31 | §4 slice map (sequencing only) | **`PP3` ran before `PP2`**, reversing the slice map's `PP0 → PP1 → PP2 → PP3` order. No decision changed. | The two slices overlap on five files, and `PP3` *deletes* two that `PP2` would *edit* (`CardReveal.tsx`, `AccessoryUnlockModal.tsx`, plus `equippedAccessories.ts`, `useRunShow.ts`, `Plan.tsx`) — parallel worktrees would have produced delete/modify conflicts, and the mapped order would have migrated `FaceIcon` into components about to be deleted. Cutting first shrank `PP2`'s call-site list instead. |
| 2026-08-31 | P4 (depth), noted not changed | **`briefing_featured_kartu_voice` still generates for the deleted Today panel** and is left for `W2`, with the reasoning recorded rather than acted on. | The row is keyed on the featured card id, so it re-bills when the pick *changes*, not nightly. Stopping it means deleting the dispatch block at `DailyBriefingCommand.php:44-55` (**not** the `cadence()` enum, which nothing dispatches off) plus non-mechanical surgery on three assertion sites in `DailyBriefingCommandTest`. Folding a backend behaviour change into a slice scoped "UI and routes only" is what P4 exists to prevent; if `W2` slips, this goes in its own one-commit PR. |

| 2026-09-01 | new (P36) | **Card radius settled at `rounded-md` (14px) app-wide for cards.** `PS4` used `cardVariants` where Plan had previously used shadcn `Card` at `rounded-4xl` (26px); flagged as a cross-screen precedent rather than a slice-local call, and ratified by the user merging #688. | The first screen port necessarily sets it for the other ten. Leaving it implicit would have had `PS2`, `PS3`, `PS5`-`PS7` and `PS9`-`PS11` each inherit it by accident, or diverge, with no record of when it was decided. |

| 2026-09-01 | 15 | **P15 named the wrong screens.** It was being read as applying to Profile; `ProfileScreen.tsx` contains no badge reference anywhere, and its only chip row (`:514-521`) is two *stat* chips — a delta chip and a goal chip — which already wrap. P15 now names `PS6` and `PS9` explicitly, the two badge surfaces P14 actually keeps. | Found by `PS10`, which was briefed with P15 as one of its decisions and checked it against prototype source rather than implementing it. Corrected before `PS6` and `PS9` were briefed, so neither inherits the error. Tenth factual error in this plan caught this way. |

| 2026-09-01 | new (P37, P38); `PP4`, `PS12`, §5 | **First post-port browser sweep, and two new decisions out of it.** All eleven screens shot at 390/320/1280/1536 and reviewed against prototype source. Zero horizontal overflow, zero console errors. Two divergences needed a user ruling and became **P37** (port the prototype's lowercase literally, app-wide, superseding `docs/voice-and-tone.md`'s Title-Case rule) and **P38** (`RecapCard` keeps six metrics, restyled onto the prototype's chip shape). Seven further single-screen divergences were confirmed and scoped as **`PS14`**; the lowercase pass is large enough to be its own slice, **`PS13`**. `PP4`'s row grew from one known gap to seven. **Two bugs in the sweep tooling itself were found and fixed**, both meaning earlier "no browser pass" gaps were worse than recorded: activity detail was dropped from *every* sweep (the resolver scraped `/activities`, which is not a route — History owns the feed), and Login was captured at exactly one viewport height, losing half the page. | The sweep was recommended because four slices (`PS7`, `PS8`, `PS10`, `PS11`) shipped without one, each rightly judging a contended screenshot worse than an honest gap — but the gap was cumulative, and this program exists because the app *looked* wrong. Four agent findings were dismissed after live DOM checks rather than fixed: a Trends chart "rendering as a solid grey rectangle" (the canvas holds 743 distinct colours and both lines), Profile's mobile stat strip "missing" (all five tiles laid out in a horizontal scroller), Login's `WHY_GET` "splitting into cards on mobile" (one `rounded-2xl` container with `divide-y`), and "stray text at the top of Login" (absent from the DOM). Each would have cost an implementation pass. Activity detail came back **at parity with zero findings** after its reviewer verified three apparent gaps against source and found all three were correct real-data gating. |

| 2026-09-02 | 38; `PS14`, §5 | **`PS14` verified before implementation; half its recorded items were wrong.** P38's premise does not survive: `WeeklyStatusChips`' base chip class is byte-identical to the prototype's, so "redraw onto the prototype's chip shape" is a no-op, and the live divergences are colour and row layout instead. Login's kartu teaser is a different object rather than a differently-sized one. Today's phase badge is not uppercase for a reason that reaches every `Chip` in the app, not one call site. History's bell does not exist in the app at all. Two of these needed a user ruling and got one; a third item was added after finding that `demo:seed` seeds no `RaceGoal`. | The pattern this program keeps re-learning: the sweep that *found* these items reported what it saw, and what it saw was accurate — the descriptions written from it were not. Four wrong out of eight is the highest rate yet, and the one that mattered most was invisible to source-reading entirely: `text-label-micro` is silently stripped by tailwind-merge, so the source says uppercase and the browser says none. |

| 2026-09-02 | `PS12`, §5 | **`PS12`'s three items verified before implementation; two changed premise.** The regenerate control turns out to be two different affordances sharing a name, of which only the narration reread is `AnalysisStatus`'s — and the prototype is byte-identical about that one across the two screens that draw it. The empty state's last remaining constant does not exist: the prototype draws both 40 and 48. And on the tint, one of the three named controls carries no tint at all while a fourth, unnamed call site is already correct at `/10` and would have been broken by the sweep as written. | Third slice running to the same pattern, and the sharpest version of it yet: the recorded item was not merely imprecise, it named a change that would have introduced a regression. The unit of meaning on a token sweep is the control, not the value. |

---

## 6b. Raised, not yet scheduled

Things the user has asked for that are **not** parity items and need a slice of their own. Kept
here so they are not lost between waves.

| # | item | state |
|---|---|---|
| T1 | **A custom date picker.** The date field opened Chrome's native popup, which CSS cannot theme. The prototype uses a native `type="date"` too (`RaceGoalScreen.tsx:300`), so this is an **addition**, not a port. **Ruled**: a token-styled calendar on pointer devices, the native input kept on touch. **The input is kept rather than replaced** — `RaceGoalForm` is a real `<form onSubmit>`, so `required` and `min` run the browser's own constraint validation and a button would have dropped that silently. Two bugs found on the way: `Icon` renders **nothing** for an unmapped key, so `mdi:calendar` (it is `mdi:calendar-blank-outline`) shipped the trigger invisible — now guarded in `structure.test.ts`, and **the first version of that guard was vacuous**, matching only single quotes when a JSX attribute writes double; the fixed guard then found a pre-existing `mdi:alert-outline` in `RaceGoalForm` that had been rendering as nothing. `UsageFilters` stays native under P20. [T1-date-picker](slices/T1-date-picker.md) | merged, [#706](https://github.com/nukipratama/temari/pull/706), f766e2db |
| T2 | **The content column and type scale at large viewports.** Raised by the user 2026-09-02 against a ~2300px screenshot, then extended by them mid-implementation to the type. **Amends the corrected P5/P31**: 760px is the prototype's own value, and its `PhoneFrame` never renders wider than the frame, so above 900px there is no drawn reference and the choice is ours. **Ruled**: one more step, to **1040px at 1280px**, keeping the single centred column. Measured first, the type turned out **identical from 900px to 2324px** — the display tiers are fluid on `vw` but hit their `clamp()` ceiling well below 1280, and the body tiers split 87 rem-based files against 91 px literals ported 1:1 from the prototype, so a root step alone would have scaled half the UI. **Ruled**: convert the literals to rem (every value exact, so nothing below 1280 moves) then step the root 10% at the same breakpoint. The width also stopped being a literal in nine places; the nav pill deliberately holds at 760, on P32's own reasoning. Landed at **1040px / +20%** after two review passes: 10% read still too small, and the nav pill's 760 cap was overruled — P32's objection was to a *full-bleed* track, which a bounded 1040 is not. **Two defects surfaced by the step, both older than it.** (1) This theme names ten spacing steps and declared them all in **px** while every unnamed step falls through to Tailwind's rem default, so the scale was half px and half rem — agreeing at a 16px root, diverging the moment it stepped. This slice had claimed the opposite; corrected, and the ten steps plus the five `--pad-*` roles are now rem. (2) `WeekVolumeChart` pinned `h-16` around 77px of content, so it had spilled over its own legend since it was written; the step only widened it. A sweep of the nine main pages for the same pattern found no other instance. Also carries the user's cut of the top bar's Strava sync badge, checked first because its `revoked` state was a live reconnect link — Profile's hero action and the feed's `StravaSyncButton` both keep that path. [T2-wide-viewport](slices/T2-wide-viewport.md) | merged, [#704](https://github.com/nukipratama/temari/pull/704), c5eea845 |
| T3 | **The `empty` card tone.** Raised in `PS12`, and the finding is stronger than the dashed-vs-solid it was recorded as: **the prototype gives empty states no distinct treatment at all** — `EmptyInboxCard`, `NoRunsCard`, `NoRaceState` and `NoPlanState` carry byte-for-byte the same class string as its ordinary cards. The app's dashed edge and 40%-opacity fill were both invented here. `empty` becomes `border border-border-strong bg-card shadow-e1`; it stays a distinct tone rather than collapsing into `card`, which uses the lighter `border-border`. [T3-empty-card-tone](slices/T3-empty-card-tone.md) | merged, [#705](https://github.com/nukipratama/temari/pull/705), 6b2bf21a |
| W1 | IA cutover | merged | [#707](https://github.com/nukipratama/temari/pull/707) | main | Wave 3 opens. **Three of its four items needed no work, and that is the finding**: routes carry no cut surface, `C1` had already pulled every legacy 301 forward, and `nav.ts` satisfies P6, P33 and P35 as `F4` landed it (verified against each decision, pinned by eight tests). A cross-check of every `Inertia::render` target found no missing component and no unreachable page. **The fourth was a real gap**: `ROUTE_BUDGETS_KB` guarded four routes picked before the port, and measured after it, **History (200.4), Settings (199.6) and Plan (196.0) were all heavier than the budgeted Profile (185.8) while being unguarded entirely**. The list is now one entry per prototype screen, with every budget *tighter* than the one it replaces — a re-baseline within P34, not a weakening, and proven to still fail on a breach. [W1-ia-cutover](slices/W1-ia-cutover.md) | Squashed as fe5bebfe. **Also uncovered that three PRs merged with red CI**: `T2` (#704) broke a doc citation by deleting `StravaSyncBadge`, `repo-guards` failed, every downstream job skipped, and `gh pr merge --auto` merged it anyway; `T3` (#705) and `T1` (#706) branched off after and inherited it. `--auto` waits only for **required** checks, and no branch protection marks any check required. The citation is fixed here; the branch-protection half is the user's, raised 2026-09-02 and not yet settled. Merging is manual until it is.
| T4 | **`resources/brand/tokens.css` and `tokens.html` are stale and unread.** Both still hold the pre-Pewter gold-and-indigo palette; nothing imports either. Found in `PP5`. Belongs to `W2`'s dead-code sweep. **Premise corrected in `W2`**: `css()` reads the live `COLOR` map, so these are stale *output*, not output from a stale script — the dead half is the generator's emit functions. | swept by `W2` |
| W2 | Dead-code sweep | **Ruled by the user: code *and* schema**, plus two maximal scope calls — the whole unread `resources/brand/` preview layer, and all seven test-only frontend modules including the documented `GradientText` / `HeroPanel`. **The schema ruling bought less than it promised: there are no dead tables**, only two dead columns (`users.pending_reveal_card_id`, `user_unlocks.equipped`), and persona mix / relative effort / dawn-shift are backed by no column at all. **`PP3`'s nine-item handoff was three-for-five** on the items it called dead — `strainMonotonyTrend()` and `PrCategory::efforts()` are both live, each read by a narrator tool or the Riegel regression rather than by a screen, which is the failure mode: a metric can lose its chart and keep its consumer. The featured-kartu pipeline was the one item still *billing*. [W2-dead-code-sweep](slices/W2-dead-code-sweep.md) | merged, [#708](https://github.com/nukipratama/temari/pull/708), c3f58da0. Also settled two open questions after review: the five unread scale maps came out of `build-tokens.mjs` (and both docs stopped claiming `app.css` is *generated* by it, which had been false since the emit half went), and the leftover `prototypes/` dist was removed. |
| T5 | **Branch protection is split across two systems, and nobody chose that.** Raised by the user 2026-09-02 after asking where `main`'s ruleset lives — it does not have one. `main` is guarded by **classic branch protection** (`required_status_checks: [ci-gate]`, `strict: false`, `enforce_admins: true`, `required_approving_review_count: **0**`, force-push and deletion blocked, `required_conversation_resolution: false`), while `epic/*` is guarded by **ruleset `22084849`** created during `W1`. They are separate GitHub features with separate UIs — Settings → Branches vs Settings → Rules → Rulesets — so a change to one silently does not apply to the other, which is exactly the split-brain that let three PRs merge with red CI before `W1` caught it. Things to settle: whether to consolidate onto rulesets; whether `main` should require an approval at all (it requires none today, so CI green is the only gate); whether `strict` should be on (it is off, meaning a stale branch can merge untested against the current base — harmless right now only because `main` is **zero** commits ahead of the epic); and whether the `epic/*` ruleset should outlive this program or be retired with it. Not a parity item, and deliberately not folded into `W7`. | **settled 2026-09-03: consolidated onto rulesets.** `main`'s classic protection was replicated 1:1 into ruleset `22128658` (PR required, `ci-gate`, no force-push, no deletion, **no bypass actors** — the ruleset equivalent of `enforce_admins`), verified against the old settings *before* the classic rule was deleted, so protection was never absent. Kept as two rulesets rather than one: `main`'s deletion-block and PR requirement should not apply to epic branches, which need to be deletable once merged. **Required approvals stay at 0 deliberately** — GitHub does not allow approving your own PR, so any higher number would hard-block a solo maintainer; `ci-gate` plus no-bypass is what actually guards the branch. Note for later: `GET /branches/main/protection` now returns *"Branch not protected"* because that endpoint only knows about classic rules — `GET /rules/branches/main` is the authoritative one. **Amended 2026-09-03:** the `epic/*` ruleset gained an explicit `pull_request` rule too, so the two now match on PR-required / `ci-gate` / 0 approvals / no bypass. Before that, direct pushes to an epic failed only *incidentally* — a freshly pushed commit has no check runs, so the required check could not be satisfied — which would have stopped working the moment someone pushed a commit already built on another branch. `main` still additionally blocks force-push and deletion; epic branches deliberately do not, since they must stay deletable once merged. |
| W4 | Docs | **Two of the stub's six targets needed no work, and that is the finding** — `plan-periodizer.md` verified clean across 15 source files, `CLAUDE.md` already fixed by `W6`, and `docs/features/` audited in full (25 notes) and clean, disproving the expectation that the port had left a tree of notes describing deleted screens. The substantial find was the **citation guard's three blind spots**, the third unpredicted: filename-as-link-text (65 citations), PascalCase link text, and `checkLineDrift()` resolving against the repo root only while `checkCitation()` resolves against root *and* doc directory — so **41 relative-path citations passed the existence check and skipped the drift check entirely**. Closing all three put ~130 citations under coverage and immediately failed on **six real defects, three pointing past the end of their file**, which also **corrects the features audit** (it verified paths existed, not lines). `DESIGN.md` and `frontend-architecture.md` both still declared the app *light-mode only*; the dark-default ADR (decision 6) was written; four dead `--color-phase-*` tokens deleted and pinned in `DesignTokenDocsTest`, proven non-vacuous by tripping on the slice's own first draft; `T2`'s px-font-size question closed with two rules that started **red**. [W4-docs](slices/W4-docs.md) | merged, [#711](https://github.com/nukipratama/temari/pull/711), 10e98ba6 |
| W5 | Merge readiness | Deletes the frozen prototype (51 tracked files / 728K; the 276M on disk was untracked `node_modules`), verified against the `prototype-frozen` tag on origin so the 144 `plan/` citations stay resolvable. **The gate `W5` exists to run could not run**: `composer check` died at step 13 of 16 with **all 3604 tests passing**, because git refuses the bind-mounted repo as dubious-ownership and Pest reports that with the same message as a missing binary — so `test:coverage`, `build` and `check:chunks` had never run inside the gate at all. Found by instrumenting Pest's throw sites; fixed in `compose.yaml` rather than the Dockerfile because **Docker Hub is unreachable here** and an unverifiable image change is exactly what §7 of the memory forbids. Also: the sweep **could not check both grounds** as §9 instructs (`shoot.mjs` had no theme switch, now `THEME=`), zero overflow across 26 page/viewport combinations, dark ground clean, and one real light-ground defect fixed (the elevation stat label truncating at 390px, measured 71/58). **Stops at the epic** — the merge to `main` is the user's separate step. [W5-merge-readiness](slices/W5-merge-readiness.md) | merged, [#712](https://github.com/nukipratama/temari/pull/712), 5b2ded27 |
| W7 | LLM trigger map | Docs-only, and it found what the brief did not predict: **`routes/console.php:63` described `plan:regenerate` as "No LLM involved (deterministic periodizer)"** while the command calls `PlanNarrationRequester::requestForCurrentWeek()` per non-demo user — `plan_day_voice` ×7 and `plan_week_voice` with `invalidate: true`, so **re-billed every Monday by design**, plus an idempotent `plan_season_voice`. Up to nine rows per user per week behind a comment claiming zero: the same shape as `W2`'s featured-kartu finding. A second comment described the daily kickoff as dispatching six narration types where it dispatches one. Both corrected. All twelve `AnalysisType` cases are dispatched — **none orphaned**, unlike what `W2` found. The doc carries a spend query that was **run before it was written down**, and states that `kind` is the narrator rather than the origin. Its own citations were proven checked by deliberately breaking one — and they are only checkable because `W4` fixed the guard's relative-path blind spot. [W7-llm-trigger-map](slices/W7-llm-trigger-map.md) | merged, [#713](https://github.com/nukipratama/temari/pull/713), ee62cf1d |
| W6 | Indonesian pockets | **The stub was wrong in four ways.** `briefing_featured_kartu_voice` closed itself when `W2` deleted the pipeline; `rute` is not persisted (React state plus a PHP default), so no migration; **`kartu` is a second layout token *and* the product noun across 197 references**, which the stub never raises — ruled by the user as a pocket and renamed to Card; and there is **a second deployed env var the stub misses**, `AZURE_OPENAI_AKU_PROFILE_VOICE_DEPLOYMENT`, which matters more than the one it names because every narrator override falls back to the primary deployment *silently*. Also corrected: `angin` is not the only regression assertion, there are **three**. Two vacuous tests found and fixed, the second proven by inverting it. Restoring the deleted `check-indonesian.php` was offered and **declined** — `C1`'s guidance-not-a-gate call stands. [W6-indonesian-pockets](slices/W6-indonesian-pockets.md) | merged, [#709](https://github.com/nukipratama/temari/pull/709), ab77b59f |

---

## 7. Carried over unchanged

These still hold from the original program and are not re-litigated here:

- **Branching**: every slice PR targets `epic/mobile-ux-port`. No merge to `main` until the whole
  chain (this program → `epic/rebrand-temari` → `main`) is deliberately merged.
- **Tests**: the 1:1 class↔test convention and the 95% coverage gate are untouched. Deleting a
  component deletes its test with it.
- **Verification**: the full ladder in [../README.md](../README.md) §9 runs before every PR.
  `grounds.json` is regenerated by any slice touching panel backgrounds.
- **The prototype stays frozen and read-only** (decision 19). A spec change goes into the owning
  slice doc, never into the prototype. It is still deleted by `W5` at the end.
- **Coupling** listed in [../README.md](../README.md) §8 still bites, especially `grounds.json`
  across parallel worktrees.
