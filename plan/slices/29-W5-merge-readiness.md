# W5 — Merge readiness

**Wave** 3 · **Slot** main checkout · **Blockers** `W4` · **Status** todo

## Goal

Final gate before `epic/rebrand-temari` merges to `main`. Deletes `resources/brand/prototype/`
(decision 19 — its job is done, it has been read-only spec since the tag). Full `composer check`
and a full `browser-review` sweep across all eleven screens, both grounds, as the last checkpoint
before the merge that finally reaches prod.

## Files touched

`resources/brand/prototype/` (deleted, entire tree).

## Blockers

`W4`.

## Acceptance criteria

_To be filled when wave 3 starts. Should restate the wave-1 exit criteria in
[../README.md](../README.md) §9 as a final program-wide check, plus confirm nothing in
`plan/ledger.md` is left at `defer`._

## Coverage delta

_To be filled when wave 3 starts._

## Verification notes

_To be filled when wave 3 starts. This is the merge to `main` — decision 15's whole point was
keeping this off the deploy critical path until this slice says go._

## Open questions

_To be filled when wave 3 starts._
